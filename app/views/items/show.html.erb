<!-- scrollToTop -->
    <!-- ================ -->
    <div class="scrollToTop circle"><i class="fa fa-angle-up"></i></div>

    <!-- page wrapper start -->
    <!-- ================ -->
    <div class="page-wrapper">
      <!-- breadcrumb start -->
      <!-- ================ -->
      <div class="breadcrumb-container">
        <div class="container">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><i class="fa fa-home pr-2"></i><a class="link-dark" href="/"></a></li>
            <li class="breadcrumb-item active"><%= @item.title %></li>
          </ol>
        </div>
      </div>
      <!-- breadcrumb end -->

      <!-- main-container start -->
      <!-- ================ -->
      <section class="main-container">

        <div class="container">
          <div class="row">

            <!-- main start -->
            <!-- ================ -->
            <div class="main col-12">

              <!-- page-title start -->
              <!-- ================ -->
              <div class='row'> 
                <h1 class="page-title mr-auto"><%= @item.title %></h1>
              </div>

              <div class="separator-2"></div>
              <!-- page-title end -->

              <div class="row">
                <div class="col-lg-6">
                
                  <%= image_tag @item.image_url %>
                  
                  <!-- pills end -->
                </div>
                <div class="col-lg-6">
                  <h2 class="mt-4">Description</h2>
                  <p><%= @item.description %></p>
                  <hr class="mb-10">


                    <% begin %>
                      <%= form_tag(cart_item_pathath(CartItem.find_by(cart_id: current_cart.id, item_id: params[:id]).id), method: "patch") do %>
                      <div class ='row align-items-center'>

                          <span>Quantity</span> <input type="text" class="form-control" value=" ">
                          <%= hidden_field_tag 'id', @item.id %>
                          <%= button_tag(type: "submit", class: "pull-right margin-clear btn btn-default-transparent btn-animated") do%>
                           <span class="product price pull-right"> <%= @item.price %> €</span>
                            Add To Cart<i class="fa fa-shopping-cart"></i>
                          </div>
                          <% end %> 
                        <% end %>
                    <% rescue %>
                      <%= form_tag(cart_items_path, method: "create") do %>
                       <div class ='row_description'> 
                          Quantity <%= text_field_tag 'quantity' %>
                          <%= hidden_field_tag 'id', @item.id %>
                          <%= button_tag(type: "submit", class: "pull-right margin-clear btn btn-default-transparent btn-animated") do%>
                            Add To Cart<i class="fa fa-shopping-cart"></i>
                          </div>
                          <% end %> 
                        <% end %>
                      <% end %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <!-- main end -->

          </div>
        </div>
      </section>
      <!-- main-container end -->
    </div>
    <!-- page-wrapper end -->